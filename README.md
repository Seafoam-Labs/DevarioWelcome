# DevarioWelcome

A GTK4 desktop application built with Zig and [zig-gobject](https://github.com/ghostty-org/zig-gobject) bindings.

This app serves as a launcher for custom scripts and tooling that run directly from the UI.

## Building

```sh
zig build
```

## Running

```sh
zig build run
```
