# DevarioWelcome

A GTK4 desktop application built with Zig

This app serves as a launcher for custom scripts and tooling that run directly from the UI.

## Building

```sh
zig build
```

## Running

```sh
zig build run
```
